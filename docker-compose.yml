services:
  mongo:
    image: mongo:7
    container_name: omichef-b2b-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - omichef_mongo_data:/data/db

volumes:
  omichef_mongo_data:
